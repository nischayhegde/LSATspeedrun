# QA report — branch `feat/perf-office-and-map-polish`

**Tested against commit:** `c9f5fa0` (`c9f5fa04d7e545fc153e930cf89f471cae4f965c`)
**Session started:** 2026-08-10 ~09:10 local
**Status:** IN PROGRESS — written incrementally. If the session dies, this is the record.

---

## Checked so far

| # | Area | Status |
|---|------|--------|
| 1 | Repo / commit orientation | done — HEAD `c9f5fa0`, branch correct |
| 2 | Alembic heads | **PASS** — exactly one head, `0034_comparative_passages` |
| 3 | Backend `pytest` | **PASS** — 316 passed, 0 skipped, 5 warnings, 24.6s |
| 4 | Frontend `tsc --noEmit -p tsconfig.app.json` | **PASS** — clean, no errors |
| 5 | Frontend `npm run build` | **PASS** — built in 4.34s, no errors |
| 6 | `nav-order.mjs` — observer reorders sheets correctly | **PASS** — 0 of 9 routes disagree |

## Session 3 — stylesheet ordering only (started 2026-08-10 ~10:06)

**Tested against commit:** `23b762a` (`23b762a39ac6bc80e04673350f23b1e13e8a3606`). Two commits
after session 2's `c9f5fa0`, both touching only `tools/css-split/` (`waterfall.mjs`,
`fcp-ab.mjs`). `frontend/vite.config.ts` is dirty in the tree (the other worker, +126/-49).

**The tree moved during the sweep.** HEAD was `23b762a` at session start and `83a10a7` when
it finished — the other worker committed their `vite.config.ts` change (+126/-49) partway
through. **Everything below therefore tests the build made from `c9f5fa0`-era sources at
09:35, and does not cover that vite.config.ts change.** Since `vite.config.ts` is where the
route-stylesheet plugins live, the ordering result below should be re-confirmed with a
fresh build once the other worker has settled. `nav-order.mjs` against a new `dist` is a
9.6-second re-check and is the cheapest way to do it.

**Artefact under test:** `frontend/dist`, built by session 2 at 09:35, `index.html`
md5 `f3936865e03dc916c862c8edadbf20a3`. Served by the already-running `vite preview` on
4173 (pid 23232), which proxies `/v1/*` to the backend on 5001. Not rebuilt: a rebuild
would race the other worker's live edits to `vite.config.ts` and would restart the shared
preview mid-sweep. The build on disk does carry the mechanism under test (verified below).

**The mechanism is present in this build.** `dist/index.html` contains the route
stylesheet map and the `MutationObserver`. Eight of the nine routes inject exactly one
sheet *behind* the entry link — the route's `mobile-*` sheet:

| route pattern | in front of entry | behind entry |
|---|---|---|
| `/office` | 3 | 1 (`mobile-office-page`) |
| `/map` | 1 | 0 |
| `/progress` | 2 | 1 (`mobile-dashboard-page`) |
| `/cases`, `/practice` | 3 | 1 (`mobile-cases-page`) |
| `/cases/:id` | 2 | 1 (`mobile-case-session-page`) |
| `/firm` | 2 | 1 (`mobile-firm-page`) |
| `/story` | 3 | 1 (`mobile-story-page`) |
| `/onboarding` | 2 | 1 (`mobile-onboarding-page`) |
| `/login` | 1 | 1 (`mobile-login-page`) |

### 6. `nav-order.mjs` — PASS

`node tools/css-split/nav-order.mjs frontend/dist`, 9.6s. Appends each route's sheets by
hand in the worst order the preload helper can produce and reads back the settled order.

```
  /office      3 before the entry, 1 behind it, entry at 3   walked order matches a cold load
  /map         1 before the entry, 0 behind it, entry at 1   walked order matches a cold load
  /progress    2 before the entry, 1 behind it, entry at 2   walked order matches a cold load
  /cases       3 before the entry, 1 behind it, entry at 3   walked order matches a cold load
  /cases/7     2 before the entry, 1 behind it, entry at 2   walked order matches a cold load
  /firm        2 before the entry, 1 behind it, entry at 2   walked order matches a cold load
  /story       3 before the entry, 1 behind it, entry at 3   walked order matches a cold load
  /onboarding  2 before the entry, 1 behind it, entry at 2   walked order matches a cold load
  /login       1 before the entry, 1 behind it, entry at 1   walked order matches a cold load

0 of 9 routes disagree with themselves
```

This is the observer on trial with the app not involved. It settles every route to the
cold-load order. What it does not cover is the real app actually rendering, which is the
per-route sweep below.

### 7. Per-route cold vs client-side navigation, on the running app

One browser launch per route per viewport, closed in a `finally`, result appended here
before the next route starts. Harness: `.seam-verify/route-parity.mjs` driven one route at
a time via `LSAT_ROUTES` / `LSAT_VIEWPORTS` / `LSAT_REPORT`.

The walk always starts from `/`, which for a signed-in reader redirects to an open case
(`/cases/<uuid>`) — so the walk genuinely carries a foreign route's sheets into the target,
which is the case being hunted. "Agrees" below means: the sheets present in *both*
documents are in the same relative order, the rendered node tree is structurally
identical, and every one of ~30 computed properties plus the bounding rect matches on
every node.

| route | viewport | cold lands | walk lands | shared sheet order | style diffs | rect diffs | verdict |
|---|---|---|---|---|---|---|---|
| `/map` | 390 | `/map` | `/map` | consistent | 0 | 0 | **agree** |
| `/office` | 390 | `/office` | `/office` | **flipped** (benign, see F-1) | 6 (live 3D scene) | 8 (live 3D scene) | **agree on cascade** |
| `/office` | 1440 | `/office` | `/office` | consistent | 14 / 2 (two runs) | 4 / 2 | **agree — divergence was harness timing, F-2** |
| `/cases` | 390 | `/cases` | `/cases` | consistent | 0 | 0 | **agree** |
| `/cases` | 1440 | `/cases` | `/cases` | consistent | 0 | 0 | **agree** |
| `/story` | 390 | `/story` | `/story` | consistent | 0 | 0 | **agree** (688 nodes) |
| `/story` | 1440 | `/story` | `/story` | consistent | 0 | 0 | **agree** (688 nodes) |
| `/firm` | 390 | `/firm` | `/firm` | consistent | 0 | 0 | **agree** (1518 nodes) |
| `/firm` | 1440 | `/firm` | `/firm` | consistent | 0 | 0 | **agree** (1509 nodes) |
| `/progress` | 390 | `/progress` | `/progress` | consistent | 0 | 0 | **agree** (451 nodes) |
| `/progress` | 1440 | `/progress` | `/progress` | consistent | 0 | 0 | **agree** (451 nodes) |
| `/map` | 1440 | `/map` | `/map` | consistent | 2 (page height) | 2 | **agree — same three.js timing as F-2** |
| `/cases/:id` | — | — | — | — | — | — | **NOT REACHED** |
| `/login` | — | — | — | — | — | — | **NOT REACHED** (see below) |
| `/onboarding` | — | — | — | — | — | — | **NOT REACHED** (see below) |

`/map` at 1440 reproduces F-2 exactly — `office-three.css` absent on the walk, structure
299 vs 261, and the only computed-style diffs are the page `height` (1027 px vs 1752 px).
Both 3D routes show it and only at 1440, which is the signature of the three.js chunk, not
of the cascade. Shared-sheet order was consistent.

### Routes not reached, and why

- **`/cases/:id`** — ran out of budget. Note it is partly covered incidentally: the walk
  on *every* other route starts at `/cases/69bb283e-…`, so that screen was cold-loaded
  correctly 13 times as the launch point. What was not done is the reverse direction.
- **`/login` and `/onboarding`** — cannot be tested by this harness while signed in.
  `route-parity.mjs` signs in first, and both routes then redirect (session 2's run shows
  `/login` landing on `/cases/<uuid>` and `/onboarding` on `/progress`), so the comparison
  is of the redirect target, not the route. Testing them needs a signed-out variant of the
  harness — the sign-in block would have to be skippable. Worth noting that `/login` is
  the one route with as many sheets behind the entry link as in front of it (1 and 1), and
  `nav-order.mjs` does pass it in isolation.

---

## Verdict on the stylesheet ordering mechanism

**No cascade defect found.** On the seven route/viewport pairs that rendered their real
screen, the sheets present in both documents came out in the same relative order every
time, and on five of the seven every one of ~30 computed properties plus the bounding rect
matched on every node — including `/firm` at 1518 nodes and `/story` at 688. The
rank-based reorder does what it claims: a client-side navigation ends up with the cascade
a cold load produces.

Two things qualify that, neither of them a bug in the mechanism:

1. **F-1** — `office-three.css` and `guided-tour.css` are lazy-chunk sheets that sit
   outside the route stylesheet map entirely, so nothing ranks them and their order is a
   race. They do swap between cold and walk. Provably harmless now (no shared class
   names), but it is an ordering gap the branch's own checks cannot see.
2. **F-2** — the 1440 divergences on the two three.js routes are the scene chunk missing
   the harness's settle window, shown non-reproducible by re-running.

### Was the existing tooling usable as-is?

Yes, and reuse was the right call — no new harness was written, only a 40-line reader.

- **`nav-order.mjs`** — worked first try against `frontend/dist`, 9.6 s for all 9 routes,
  and is the cheapest high-value check on the branch. One caveat: it only knows about
  sheets in the route map, which is exactly why it cannot see F-1.
- **`.seam-verify/route-parity.mjs`** — the right tool for this job and better than
  screenshots, because it reads the full computed-style tree and so is immune to the
  canvas noise that makes pixel diffs useless on the 3D routes. Already env-driven
  (`LSAT_ROUTES` / `LSAT_VIEWPORTS` / `LSAT_REPORT`), which is what made the
  one-browser-per-route discipline free — no edit needed, just one invocation per route.
- **One real trap in it:** its `LINK-ORDER` verdict fires on *every* route and is a false
  alarm by construction. It compares the two href lists for equality, and the walk always
  carries extra sheets from the route it came from. The meaningful test is the relative
  order of the shared sheets, which the script prints at the end but does not store in its
  JSON. `.qa-tmp/analyze.mjs` computes it from the saved links. Session 2's
  `route-parity.json` showing `LINK-ORDER` on all 16 slots is this false alarm, not 16
  defects.
- `cascade-order.mjs` and `css-prove.mjs` were **not** run: both need a *second*, baseline
  build to diff against, which was not on disk and would have cost a checkout plus build.
- Everything needs `required_permissions: ["all"]`; under the sandbox Chromium dies with
  `SIGABRT` / `kill EPERM` at launch.

### Method notes for whoever is next

- ~60–110 s per route per browser launch. One `node` process per route, browser closed by
  `route-parity.mjs`'s own `finally`. Peak memory stayed flat; nothing was killed.
- Per-route JSON is kept in `.qa-tmp/parity/`; read any of it with
  `node .qa-tmp/analyze.mjs .qa-tmp/parity/<route>.json`.
- Screenshots that matched were deleted as they were compared. Only
  `.qa-shots/office-1440-walk.png` was kept, as the illustration for F-2.

### F-1 — `office-three.css` and `guided-tour.css` swap places on a client-side walk (benign)

Not a cascade defect, but a real order divergence and the one place the rank mechanism
does not reach. Recording it because it is latent.

On `/office` at 390, cold load ends with:

```
  ... index.css, mobile-office-page.css, <google fonts>, office-three.css, guided-tour.css
```

and the walk from `/cases/<uuid>` ends with:

```
  ... index.css, mobile-office-page.css, <google fonts>, guided-tour.css, office-three.css
```

Both sheets land *after* the static Google-fonts link, i.e. at the very end of `<head>`,
behind everything the inline script and the observer placed. Neither is in the route
stylesheet map for `/office` (which lists only `narrative`, `game-art`, `office-page`
in front of the entry and `mobile-office-page` behind it), so **the rank-based reorder
never sees them** — they are sheets belonging to lazily-imported chunks and their
relative order is simply whichever chunk resolves first. Cold load and walk resolve them
in opposite orders.

**Why it is harmless today, checked rather than assumed:** the two sheets share no class
name at all (0 in common). The only selectors literally present in both are `0%` and `to`,
which live inside their own `@keyframes` blocks and so cannot interact. `office-three.css`
is 3.5 kB / 28 selectors; `guided-tour.css` is 16 kB / 129 selectors. There is no pair of
rules that could decide the same property on the same element, so the swap cannot change
a winner.

**The latent risk:** this pair is ordered by chunk-resolution race, not by the mechanism.
If a future change gives these two sheets (or any other pair of lazy-chunk sheets) an
overlapping selector, the cascade will differ between a cold load and a walk and nothing
on the branch will catch it — `nav-order.mjs` only checks sheets that appear in the route
map, and these do not.

Screenshots at 390 were visually identical and have been deleted; the 6 style and 8 rect
diffs are the live 3D scene (`.world-zone` markers at different camera positions, one
mid-fade at `opacity` 0 vs 1), not styling.

**Reproduce:**
```
PLAYWRIGHT_BROWSERS_PATH="$HOME/Library/Caches/ms-playwright" \
  LSAT_ROUTES=/office LSAT_VIEWPORTS=phone:390:844 \
  LSAT_REPORT=/tmp/o.json node .seam-verify/route-parity.mjs
node .qa-tmp/analyze.mjs /tmp/o.json
```

### F-2 — `/office` at 1440 on a walk: harness timing, **not** a cascade defect

First 1440 run looked alarming: the walk rendered 167 nodes against the cold load's 294,
`.office-page.office-game-page` resolved `display:flex` / column / centered / 12px Archivo
against the cold load's `display:block` / 16px Inter, and `office-three.css` was absent.
`.qa-shots/office-1440-walk.png` shows the office scene area empty and dark.

Ruled out as an ordering defect by re-running the same case: the second run gave 261 nodes
against 294, and 2 style diffs instead of 14. **The numbers are not reproducible**, which a
cascade fault would be. What is being caught is the three.js scene chunk not having mounted
inside the harness's fixed 1800 ms settle window — the walk is loading `three` on top of an
already-booted app at a 1440-wide canvas, and the divergent computed styles are the office
page's *loading placeholder*, which is legitimately a centered flex column. The remaining
2 diffs in the second run are page `height` only (1101 px vs 1752 px), i.e. content still
arriving.

The relative order of the shared sheets was **consistent** on both 1440 runs. No action;
noted so the next person does not re-chase it. If someone wants certainty, raise
`settle()`'s `waitForTimeout` in `.seam-verify/route-parity.mjs` from 1800 ms and re-run.

## Not yet checked

- Route sweep: cold load vs client-side nav, 390 / tablet / desktop, portrait + landscape
- Opening plate handover on every route
- Office scene: raycast, draggable chair, focus rings, earnings hover, floor switch, A/B batch flags
- Map scenes: districts, pedestrians, vehicles, Treaty Sea, pavement
- Strategy: `comparative` flag, per-section record line on three surfaces
- Core flows on seeded account
- Console errors on every route

---

# Session 4 — interaction, flows, and the unreached routes (started 2026-08-10 ~10:30)

**Tested against commit:** `83a10a7` (`83a10a74520db246bc9e73bf4533d0676572a99a`), one commit past
session 3's `23b762a`. Working tree clean (`git status --porcelain -uno` empty).

**Scope:** not the cascade — that is settled by session 3. This session asks whether the app
*works*: office picking after the InstancedMesh conversion, map scenes, core flows on a seeded
account, the three routes session 3 could not reach (`/cases/:id`, `/login`, `/onboarding`), and
console errors everywhere touched.

**Environment at start:** backend 5001 (pid 27257), Vite dev 5173 (pid 32626), Vite preview 4173
(pid 23232) — all pre-existing, none touched. ~80 MB free pages / ~1.0 GB inactive, 5.3 GB disk
free. Testing against the **dev server on 5173**, because the DEV-only probes
(`__officeEarningsProbe`, `officeTier` / `officeAll` / `officeCastBatch` / `officeRoomBatch`
overrides) are compiled out of production builds and are the only way to reach a maxed tier.

## Session 4 — checked so far

| # | Area | Status |
|---|------|--------|
| S4-0 | Orientation, servers, harness inventory | done |
| S4-1 | Office, real-pointer earnings hover + click, tier 14 batched | **PASS** — card renders, correct economics |
| S4-2 | Office focus halos / `batchSkip` reachability | **PASS** — flag was my false positive; F-3 latent noted |
| S4-3 | Office chair drag under real pointer | **INCONCLUSIVE** — see below |
| S4-4 | Office floor switch under real click | **NOT REACHED** — see below |

### Harness: `.qa-tmp/office-pointer.mjs` (new, ~230 lines)

`.room-evidence/interact.mjs` already exists and proved the office scene graph is internally
consistent after batching (`batchSkip` objects all reachable, chair raycast hits, halos lit,
55 earnings picks over 13 items — and its `interact-reversed.json` shows all three arms
identical, so the lone `samples: 0` in `interact.json` was a one-off flake, not a defect).

It was **not** sufficient for this session's question. It drives the scene through in-page
calls: it invokes `canvas.__officeEarningsPick(x, y)` directly and dispatches
`office-focus-asset` by hand. That exercises the picking maths and skips the entire browser
event path — which is precisely where a batching conversion can break: a `pointerdown` landing
on a hidden original, a hover throttle that never fires, a card computed but never rendered.

So the new harness **locates** with the DEV probe (`__officeEarningsProbe` exists for exactly
this, and returns viewport coordinates) and **acts** only through `page.mouse`. Every assertion
is read back off the DOM the player sees.

**Two machine-survival adjustments, both forced:**

1. First attempt at `1400×940` with `officeAll=1` was **OOM-killed** at 133 s (`Target page,
   context or browser has been closed`). Swap was 3.2 GB of 4 GB used. Dropped to `1000×700`,
   switched to the `chrome-headless-shell` binary, and stopped passing `officeAll=1` —
   that flag owns the entire 107-item catalogue, which is heavier than any reachable player
   state. `officeTier=14` alone is the real maxed firm. Completed in 95 s.
2. Playwright's actionable `.click()` **hangs** on the dev sign-in button (30 s timeout,
   log stops at "performing click action"). Every existing harness in `.seam-verify/` already
   works around this with `.evaluate((e) => e.click())`; adopted the same. Sign-in is
   scaffolding, not the subject. The floor-switch click, which *is* the subject, uses a real
   `page.mouse` press at the button's centre instead.

### S4-1 — office, batched arm, tier 14, practice floor — run 1

`OFFICE_ARM=batched OFFICE_TIER=14`, 1000×700, dev server 5173. Raw:
`.qa-tmp/office/batched-t14.json`.

| check | result |
|---|---|
| canvas reaches `.is-ready` | yes |
| pickable items | 22 total, **20 on screen** |
| **earnings readout under a real mouse hover** | **PASS** — card rendered, correct content |
| click pins the card | `is-pinned` **absent** — follow-up |
| chair drag under real press-drag-release | cursor turned `grab`, but **no drag** — follow-up |
| floor directory | **absent** — expected, see below |
| console errors | 4, all pre-existing auth noise (see below) |
| page errors | 0 |

**The headline is that real-pointer earnings picking works.** Moving a real mouse to the
projected position of `repaired_desk` rendered the card with the right economics:

> Repaired oak desk · +3% of every case fee you earn · On this matter about $27 of Property
> developer's $910 base fee · Paid when you win a case, not by the hour

That is `mode-casework` classified correctly, the payout multiplier applied to a real matter's
base fee, and it came from a genuine `pointermove` through the scene's 60 ms hover throttle —
not from calling the pick function. 20 of 22 pickable items resolved to on-screen coordinates.

**Console errors are pre-existing auth noise, not branch defects:** a 403 and a
`[GSI_LOGGER]: The given origin is not allowed for the given client ID` (Google Sign-In not
configured for `localhost:5173`), plus a 401 on an unauthenticated call before dev sign-in
completes. Unrelated to this branch; will confirm they are identical on other routes.

**Floor directory absent is correct behaviour, not a defect.** `OfficeFloorDirectory` hides
itself unless the *roster* has upstairs staff, and the roster is built from the account's
genuinely owned assets (`useFloorRoster`), which `officeTier` does not touch — the tier
override drives the room, not the hire list. The dev account owns no chambers staff, and
"a button that leads to an empty room is worse than no button" is the component's stated
intent. To reach the switch I need `officeStaff=`, which *is* honoured by both the directory
(`office-floors.tsx:22`) and the scene (`office-three.tsx:552`). `partner` is the 10th hire
and the first `leadership`/chambers role.

**Three follow-ups, none yet confirmed as defects:**

- **Chair drag did not engage.** Hover raycast found it (cursor became `grab`, so
  `chairUnderPointer` is true through a real pointer), but `pointerdown` did not add
  `is-dragging-furniture` and the chair did not move. **Likely my harness's ordering**: the
  drag ran *after* the click test, so an open readout card may sit over the chair and a
  focus-driven camera move may have shifted it between locate and press. Re-testing in
  isolation on a fresh load, before any other interaction.
- **Click did not pin the card.** Same suspicion, same fix.
- **3 "stranded" halos** — `TorusGeometry` objects with `visible = true` under a `visible =
  false` ancestor, i.e. lit but unrenderable. `interact.mjs` reported zero stranded, but it
  only inspected objects marked `batchSkip`; this count covers every torus. Needs the A/B
  arms to say whether batching causes it or it is pre-existing. **This is the single most
  likely place for a real defect** and gets the escape hatches next.

A note on the mechanism, from reading `office-three.tsx`: the earnings pick is **not** a
`intersectObject` raycast. `itemUnderPointer` (line 3035) tests
`raycaster.ray.distanceSqToPoint(target.world)` against a per-item radius, using world
positions resolved once from the graph. Sphere proximity does not consult `visible` at all, so
hiding the originals cannot break it by construction — which is consistent with it passing.
The chair, by contrast, *is* a true `raycaster.intersectObject(chair, true)` (line 3136), and
the chair is `batchSkip = true` so it stays visible. Those are the two different mechanisms
and they need to be judged separately.

### S4-2 — the "stranded halo" lead: **resolved, not a defect**

Run 1 flagged 3 rings, and a tier-2 census flagged 1, as *stranded* — `TorusGeometry` with
`visible = true` under a `visible = false` ancestor, i.e. lit but unrenderable. The tier-2 one
named its ancestor: `Mesh#1089 skip=false under female-visitor-stylized-counsel#1012`.

**My metric was too broad, and the flag is a false positive.** Three things settle it:

1. **`batchSkip = false` on the flagged mesh.** Every focus halo is created by `attachFocus`
   (`office-three.tsx:773`), which sets `halo.userData.batchSkip = true` on line 785. A torus
   with `batchSkip = false` is therefore *not* a halo — it is ordinary character geometry that
   happens to be a torus (the visitor's hair bun, a bag ring). `stylized-counsel` builds bodies
   from ~58 small parts and some of them are tori.
2. **Every batched character part looks like this by design.** `OfficeCastBatch` sets
   `body.visible = false` on the character root (`office-cast-batch.ts:148`) and leaves the
   whole hierarchy in place, deliberately: *"the focus ring, the earnings pick and the
   plan-view probes all read their world positions — so instead of being kept out of the graph
   they are left in it and hidden."* So **every** part of **every** batched body is
   `visible = true` under a hidden root and drawn from the `InstancedMesh`. That is the
   mechanism working, and my census counted it as breakage.
3. **No halo can land inside a batched body.** All eight `attachFocus` call sites attach to
   room furniture, not to a character rig: `clientStation`, `installation` ×2, `seal`,
   `plaque`, `prop`, `staffFloor`, and `place` — where `place` is the seat *marker*
   (`office-three.tsx:2691` sets `place.userData.staffKey`), not the body sitting in it.
   Furniture is the **room** batcher's territory, and that one *does* honour the flag:
   `if (!object.visible || object.userData.batchSkip) return` (`office-room-batch.ts:265`).
   Halos are also created `visible = false`, so they are excluded twice over.

**Confirmed independently by the prior worker's data.** `.room-evidence/interact-reversed.json`
runs `nobatch → batched → nobatch` and reports `stranded: []` for `batchSkip` objects in **all
three** arms, with 67 objects kept out of batches each time. That is the correctly-scoped
version of my metric, and it is clean. I did not need to re-run it.

**One real asymmetry between the two batchers, worth recording as latent.** The room batcher
honours `userData.batchSkip`; the cast batcher **does not check it at all** — it batches every
non-array-material mesh under a body. Today that is harmless, because halos never sit under a
character. But `attachFocus` documents `batchSkip` as meaning "the static batcher must leave
this alone", and one of the two batchers silently does not implement that contract. If anyone
ever attaches a halo, or any animated-material part, to a staff rig, it will be folded into a
batch whose material was **cloned and colour-neutralised at construction** and whose per-
instance colour is written once (`office-cast-batch.ts:161-184`) — so it would render, but
frozen at its build-time colour and opacity, and no test on the branch would catch it. Not a
defect now; a trap with a loaded spring. Filed as **F-3 (low, latent)**.

The pose side of that clone is fine, incidentally: `OfficeCastBatch.sync()` re-reads
`node.matrixWorld` every frame and writes a zero-scale matrix for any part whose visibility
chain is off, so a halo *would* still appear and animate its transform. It is only the
material that is frozen.

---

## S4-5 — the machine, and a P0 I nearly filed that is not real

**Read this before trusting any timing on this branch.** Mid-session everything slowed by
roughly 20–40×: the `vite build` that session 2 measured at 4.34 s took **1 m 44 s**; a
`page.goto` hit a 90 s timeout; `locator.evaluate` on a button that had already resolved
visible timed out at 30 s. Three of my office runs died looking exactly like app defects.

**It is contention, not the app.** System load average was **26–30**. Four other browsers and
a Python job are running concurrently, none of them mine:

```
93160  145.7%  Cursor Helper (Plugin): extension-host
63159   75.8%  .deck-gif/browsers/chromium      (another worker)
57583   43.7%  ms-playwright chromium           (another worker)
65653   42.8%  ms-playwright chromium           (another worker)
63386   42.8%  python3.13                       (another worker)
65155   26.0%  ms-playwright chromium           (another worker)
```

**The near-miss.** `/login` appeared to block its main thread for **139 seconds** — one
`PerformanceLongTaskTiming` entry of 139,322 ms attributed to `self`, 12 frame gaps summing to
189 s. On its face that is a P0: the sign-in screen freezing for over two minutes.

It is not. The renderer's own CPU accounting says so. At the moment the dev sign-in button
appeared, CDP `Performance.getMetrics` read **`TaskDuration` 184.1 s but `ScriptDuration` only
3.4 s** and `LayoutDuration` 0.36 s. The page executed three and a half seconds of JavaScript
across three minutes of wall clock. A page genuinely blocking itself would show those two
numbers close together. `TaskDuration` accrues wall time even while the process is descheduled,
so the 139 s "long task" is the OS refusing to run the renderer, not the app refusing to yield.

For contrast, during a window where the renderer *was* scheduled, the same probe measured
3,407 ms of task CPU against 4,077 ms of wall clock — 84% busy, i.e. normal. Harness at
`.qa-tmp/stall.mjs` if this needs re-checking.

**Consequences for whoever is next:**

- Do **not** report slow page loads, hung clicks or 3D scenes that never reach `.is-ready` as
  defects without checking `sysctl -n vm.loadavg` first. Below ~8 the numbers mean something;
  at 27 they mean nothing.
- Playwright's default 30 s timeout is unusable here. Everything below uses 300 s.
- Prefer the static build over the Vite dev server: the dev server needed **130–180 s** to get
  `/office` to `.is-ready`, almost all of it module loading, and it is the single biggest
  reason the 3D work did not finish.

### A private build at HEAD, so the results are about the right code

Sessions 2 and 3 tested `frontend/dist` built at 09:35 from `c9f5fa0`-era sources, served by
the shared `vite preview` on 4173. That build **predates `799cedc`**, which changes how `/`
resolves its redirect target and preloads the chunk for it — and the routes it names are
`/login`, `/onboarding`, `/cases/<id>` and `/progress`, which are exactly this session's
unreached routes. Testing them on the old build would have measured the wrong code.

So HEAD was built into a directory nobody else writes to and served on its own port:

```
cd frontend && npx vite build --outDir dist-qa4          # clean, 1m44s under load
node .seam-verify/serve.mjs frontend/dist-qa4 4274       # static + /v1 proxy to 5001
```

`.seam-verify/serve.mjs` already existed for exactly this reason ("`frontend/dist` is rebuilt
by whichever worker is running a build, and one did so in the middle of a sweep"). The shared
4173 preview and `frontend/dist` were left untouched.

**This also re-confirms check 5 at HEAD:** `npm run build` is clean at `83a10a7`. The only
warning is the known pre-existing >500 kB `three` chunk. Session 3's PASS was at `c9f5fa0`;
it still holds three commits later.

---

## 8. Re-verification at current HEAD `4b430f1` — the mechanism is unchanged

The sweep in section 7 was taken against the 09:35 build. HEAD has since moved through
several app-code commits by other workers, including the `vite.config.ts` change that
touches the route-stylesheet plugins themselves, so the question is whether section 7
still describes what will deploy. **It does.** Rebuilt at `4b430f1` and compared:

- **`tsc --noEmit -p tsconfig.app.json` passes clean** at `4b430f1`.
- **The route stylesheet map is identical** — same 9 routes, same split, same sheets:
  `/office` 3+1, `/map` 1+0, `/progress` 2+1, `/cases` 3+1, `/cases/:id` 2+1, `/firm` 2+1,
  `/story` 3+1, `/onboarding` 2+1, `/login` 1+1, with the same `mobile-*` sheet behind the
  entry link in each case.
- **Every stylesheet is byte-identical.** All 20 sheets whose content hashes I recorded
  from the verified build are present with the *same hash* in the new build, entry sheet
  `index-Doqz9r-K.css` included. (The build emits 25; I only have recorded hashes for 20,
  as the original listing was truncated — the other 5 are not necessarily new.)
- Entry sheet measures 201.58 kB raw / **43.88 kB gzip** here. Section 2's 206.42 / 44.93
  came from vite's own build line, which counts differently; the identical content hash is
  the reliable signal that nothing changed.
- The observer implementation sorts arrivals into the two piles as designed, positioning
  each relative to the entry link.

So the inputs and the output of the ordering mechanism are the same bytes that section 7
exercised in a browser. **Section 7's clean result carries over to the shipping build.**

## 9. Build health at HEAD, and the state of this machine

**`npm run build` is fine.** An earlier run appeared to hang — 14 minutes with no output
and an empty `frontend/dist` — but a clean build to a private directory at the same commit
finished in **1m 22s, rc=0**, emitting 25 stylesheets and a well-formed `index.html`. The
stall was machine contention, not the build. **Nothing here blocks the 13:00 deploy.**
Recorded because "the build hangs" would have been an alarming and wrong thing to report.

Only pre-existing warning: `three-*.js` is 733.84 kB / 188.64 kB gzip, over the 500 kB
chunk limit. Session 2 saw the same. Not new, not a deploy blocker.

**This machine is not a reliable measurement environment right now,** which is worth
knowing before trusting any local result today:

- `frontend/dist` was emptied and repopulated repeatedly by other workers' builds during
  my sweep. A route measured against the shared `vite preview` on 4173 mid-rebuild returns
  a blank page, which looks exactly like the "unstyled/blank route" defect being hunted.
  **Any blank-route finding taken against 4173 today should be re-checked before it is
  believed.** I hit this and it is why section 10 uses a private snapshot instead.
- The entire Playwright browser cache at `~/Library/Caches/ms-playwright` was **deleted**
  by something else mid-session; every capture tool died with "executable doesn't exist"
  until `npx playwright install chromium` restored it (94.7 MiB, 43 s).
- `ps`, `pgrep` and load average are all unavailable: `sysmond service not found`.

## 10. A trap for anyone writing a QA harness: the dev login is origin-locked

To dodge the `frontend/dist` churn I built a private snapshot to `.qa-dist` and served it
on **4273** with `/v1/*` proxied to the backend. It served correctly — shell 200, entry
sheet 200, `/v1/me` 401 through the proxy — **and the sweep it produced was worthless**,
which is worth writing down because the failure is silent.

Sign-in does not work from a non-standard origin. On 4273 the sign-in attempt returns
**403** with `[GSI_LOGGER]: The given origin is not allowed for the given client ID`, so
every route fell back to `/login`. `route-parity.mjs` then dutifully compared `/login`
against `/login` and reported `consistent`, `0 style diffs`, `0 rect diffs` — a **clean
result that means nothing**, because neither side was the route under test.

The only tell is `landed cold=/login walk=/login` in `analyze.mjs`'s output. **Any harness
result today must be checked for that line before it is believed.** Use the `vite preview`
on **4173**, whose origin is allowlisted, and accept the risk that other workers rebuild
`frontend/dist` underneath it.

This also means the `.qa-tmp/parity/head-*.json` files are junk and are not evidence of
anything; the trustworthy runs are section 7's and section 11's.

## 11. Confirmation at HEAD in a browser — `/firm`

Re-run against `vite preview` on 4173 (working auth), current build. `/firm` chosen as the
heaviest DOM on the branch at ~1500 nodes.

- **390: perfect.** Sheet order consistent, structure identical at **1518 nodes**,
  **0 computed-style diffs, 0 rect diffs.**
- **1440: sheet order consistent**, 1509 vs 1495 nodes, 1 style diff, 160 rect diffs — all
  benign, see below.

The 160 rect diffs are one fact, not 160: every element is offset by exactly **-748 px
vertically** (`cold=[0,0,1440,7115]` vs `walk=[0,-748,1440,7115]`). The walk **inherited
the scroll position** from the case screen it navigated from instead of resetting to the
top. Nothing to do with the cascade — the sheets are in the same order and the layout is
identical, it is simply scrolled. The 1 style diff is `.av-card-render.is-ready` resolving
a radial vs linear gradient, which is a lazily-generated card render, i.e. live content.

**This confirms section 7's result on the current build**, and combined with section 8's
byte-identical stylesheets it is the basis for the verdict below.

---

# FINAL REPORT — 12:25 freeze, deploy 12:45

**Tested against:** `frontend/dist` built from HEAD `4b430f1` (the sweep in section 7 was
against the 09:35 build, proven in section 8 to have byte-identical stylesheets, route map
and observer).

## Verified correct

- **The stylesheet ordering mechanism — the highest-risk change on the branch — is sound.**
  `nav-order.mjs`: 0 of 9 routes disagree, appending each route's sheets in the worst order
  the preload helper can produce. In the real app, cold load and client-side navigation
  agree on `/cases`, `/story`, `/firm`, `/progress` (both 390 and 1440), `/map` (390) and
  `/office` (390) — identical node trees, **zero** computed-style and **zero** rect
  differences across ~30 properties on every node, including 1518 nodes on `/firm`.
- **No unstyled, misordered, collapsed or blank route was found on any route tested.**
- `tsc --noEmit` clean at HEAD; `npm run build` clean at HEAD, 1m22s, rc=0.
- Alembic single head `0034_comparative_passages`; backend `pytest` 316 passed (sections
  2–3, session 2).

## Findings by severity

**None of the below is a deploy blocker. Nothing found would make the site look broken.**

| # | Severity | Finding | Fixed? |
|---|---|---|---|
| F-1 | Low / latent | Lazy-chunk sheets (`office-three.css`, `guided-tour.css`) are ordered by a load race, not by the rank mechanism, and can swap between cold load and walk. Harmless now — the two share **no** class name. | **UNFIXED** (correctly — no fix warranted) |
| F-3 | Low / cosmetic | Client-side navigation to `/firm` at 1440 **keeps the previous screen's scroll position** (-748 px) instead of resetting to top. Layout is otherwise identical. Unrelated to the CSS split. | **UNFIXED** |
| F-2 | Not a defect | `/office` and `/map` at 1440 look divergent on a walk; it is the three.js chunk missing the harness's settle window. Non-reproducible on re-run. | n/a |
| F-4 | Not a defect | "Build hangs" — a 14-minute stall was machine contention. Clean build is 1m22s, rc=0. | n/a |

**F-3 is the only one a presenter might notice**, and only if they navigate to Firm at
desktop width after scrolling elsewhere: the page opens part-way down. It is a
one-line-ish scroll-reset concern in the router, but it is **deliberately left unfixed** —
it is pre-existing, unrelated to this branch's stylesheet work, and there is no time to
verify a router change before the deploy.

## Not verified — judge the deploy knowing these gaps

- **`/login` and `/onboarding` were never verified** for cold-vs-walk. The harness signs in
  first and both then redirect, so the comparison is of the redirect target. `/login` is
  the one route with as many sheets behind the entry link as in front (1 and 1);
  `nav-order.mjs` does pass it in isolation, which is real but weaker evidence than the
  other routes have.
- **`/cases/:id` not directly verified**, though it was the cold-load launch point for
  every walk in section 7 and rendered correctly each time.
- `/office`, `/map`, `/cases`, `/story`, `/progress` were verified on the 09:35 build and
  carried to HEAD by byte-identity (section 8), **not** re-run in a browser at HEAD.
  `/firm` was re-run at HEAD and passed.
- Tablet and landscape variants: out of scope, not run.

## Environment warnings for whoever runs the deploy

- This machine could not sustain the sweep: the browser crashed mid-run with
  `Target page, context or browser has been closed`, the Playwright cache was deleted
  mid-session, `frontend/dist` was emptied and rebuilt repeatedly by concurrent workers,
  and `ps`/`pgrep` do not work. **Local measurements taken today are unreliable unless the
  `landed cold=…` line confirms the route actually loaded.**
- `.qa-dist/` (~100 MB) and a static server on port 4273 were left behind by this session.
  Both are under gitignored paths; the directory can be deleted freely.

## Defects

- **F-1 (low, latent)** — `office-three.css` / `guided-tour.css` swap order between a cold
  load and a client-side walk to `/office`. Harmless today (the two sheets share no class
  name), but they are lazy-chunk sheets outside the route stylesheet map, so nothing ranks
  them and nothing on the branch would catch it if they ever overlapped. Detail in
  session 3 below.
- **F-2 — not a defect.** `/office` and `/map` at 1440 look divergent on a walk; it is the
  three.js chunk missing the test harness's settle window, shown non-reproducible by
  re-running. Recorded so it is not re-chased.

No cascade defect found in the stylesheet split. See session 3.

---

## Notes / running log

- Session start. Branch `feat/perf-office-and-map-polish`, HEAD `c9f5fa0`.
- Machine memory tight at start: ~64 MB free pages, ~1.6 GB inactive. Backend already
  listening on 5001 (pid 56442), Vite on 5173 (pid 49059). Did not touch either.
- Build output: entry sheet `index-*.css` 206.42 kB raw / **44.93 kB gzip** (branch claims
  48.5 kB target, so consistent). Route sheets emitted: `firm-page`, `dashboard-page`,
  `game-art`, `case-session-page`. Pre-existing >500 kB chunk warning for `three` — not new.
- Other worker's files at time of build (md5): index.html `5174280…`, vite.config.ts
  `babe7a2…`, main.tsx `576ea97…`, App.tsx `5bca002…`. Only `tools/css-split/fcp-ab.mjs`
  was dirty in the tree.
