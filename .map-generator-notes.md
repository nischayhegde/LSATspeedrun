# READ BEFORE YOU MEASURE ANYTHING

**The map harness is not as reproducible as every document in this repository,
including the earlier sections of this one, assumes it is.** Established the
evening of the 10th, by accident, in a control run:

| what | Old Quarter footway-in-solid share |
| --- | --- |
| untouched tree, freshly started dev server | **.0021** |
| the same untouched tree, server that had hot-reloaded through six edits | **.0109** |
| the same untouched tree, a second cold server, later | **.0109** |
| the same untouched tree, a third cold server | **.0021** |

Same commit, same probe, same machine. `solids 47912` and `facades 783`
identical to the unit in every run, so the scene's static geometry is stable;
`samples` moved by one and `hits` by 28 -> 147. Something in the **footway
network** comes out differently between server lifetimes, and a five-fold
difference on a district whose whole measured range today was .0021 to .0568 is
larger than most of the effects this harness has been used to judge.

## What this invalidates

Every three-replicate result in this file, and every "measured worse, reverted"
verdict taken on one. Replicates within a single server lifetime agree to four
digits -- which is exactly why this went unnoticed for a whole day: the metric
looks perfectly deterministic right up until you restart. An arm and its control
taken in different sessions were never comparable, and at least one of tonight's
reverts (the parcel reservation, .0379 against a control that could honestly
have been .0021 or .0109) was judged on that basis.

## The protocol, from now on

1. Restart the dev server. Do not measure on a server you have edited under.
2. Take the control **in the same session, on the untouched tree**, not from a
   number written down earlier.
3. Never compare across server lifetimes.
4. Treat anything under about 5x on the Old Quarter as unresolved until the
   bimodality below is understood.

`tools/map-qa/lib.mjs` now detects the first of these and prints a HOT SERVER
warning when any file under `frontend/src` is newer than the dev server process.
It cannot detect the rest, so the protocol still has to be followed.

## The open question, which somebody should take

The two figures look bimodal rather than noisy -- .0021 or .0109, not a spread
between them -- and `samples` differing by exactly one suggests a single footway
run being emitted or cut differently. There is no `Math.random` in the map
modules, so the candidates are iteration order over a `Map`/`Set` keyed by
object identity, or something reading a real clock during the plan. Finding it
would make every number in this file mean something again, and it is probably
an hour's work with `plan-dump.mjs` diffing the network between two cold runs.

Note also that the browser began crashing outright ("Target crashed") on this
machine late in the evening, losing runs silently -- a probe that prints no
`=== region ===` line has not measured anything, and a loop over replicates will
happily report three arms when only one ran.

---

# The Circuit: pavement inside solid geometry — generator notes

Worktree `/private/tmp/lsat-mapgen`, branch `fix/map-generator`, from `73e9f3e`.
Every number here is on the **corrected per-triangle** instrument. Nothing in
this file is comparable to the old whole-mesh-AABB figures.

## Harness set-up

- Own dev server on `127.0.0.1:5373` from this worktree (`5173` is the main
  checkout's and would have measured somebody else's tree).
- `tools/map-qa/*` wrote reports to a hard-coded `/Users/alan/LSATspeedrun/.maps`.
  Every worktree on this machine therefore overwrote the same file with the same
  tag. Reports now land in the checkout's own `.maps` (`OUT` in `lib.mjs`,
  overridable with `MAPS_OUT`). Committed as `b643960`.
- The dev-login click had the default 30 s timeout, which a cold Vite dev server
  on this machine does not make. It presented as "the button does not exist" and
  cost a run; now 180 s.
- `tools/map-qa/authored.mjs` is new: it reads back the authored-parcel siting
  report described below.
- Under load (18 or more) a replicate hangs in the browser perhaps one time in
  three and produces no output at all rather than an error. Check `uptime`
  before concluding anything; re-run the replicate.

## Baselines reproduced, three replicates each

`inside.mjs`, walkers inside solids, the primary metric:

| district | r1 | r2 | r3 |
| --- | --- | --- | --- |
| The Circuit (`nation`) | .0427 | .0427 | .0427 |
| Old Quarter (`city`) | .0588 | .0720 | .0588 |
| Sovereign Arc (`continent`) | .1740 | .2085 | .1740 |

`footway-audit`, blocked share: Circuit **.1898** (43 ways, 96.7 m, 18.3 blocked),
Old Quarter .0190, Sovereign Arc .0347. Identical to the predecessor's
`footway-truebase`, so this worktree measures the thing the project is judged on.

**On the noise floor.** Re-running the *same scene* is not noisy on The Circuit
at all — .0427 to four digits, three times, and again in every later control
arm. Old Quarter and Sovereign Arc do vary run to run (about ±.015 and ±.035).
The ±.03 in the brief is therefore a floor for the two noisy districts and an
overstatement for The Circuit, where any movement is real. What is *not* small
is the spread between **scene variants**: see below.

## The one thing that worked

Wool Hall Yard's ten market stalls were never declared. `70919f4`. Old Quarter
.0588/.0720/.0588 to .0435/.0435/.0435, with The Circuit deterministic and
unchanged as a control, no ways and no footway metres lost, and blocked length
35.6 to 33.3 m. It costs nothing and it is the only arm today that improved
anything.

## The thing that actually mattered: the trees were planted on the pavement

Following the corollary above — declare what the router cannot see — the first
step was to ask *what* it cannot see, per district, from the walker audit rather
than from `undeclared.mjs`. That turned out to matter, because
**`undeclared.mjs` cannot see this defect at all**: it skips instanced meshes
and anything over twelve metres across, and it samples centrelines. Neither can
`footway-audit`, which does not move by a single decimal for the change below.
Only the per-triangle walker audit sees it.

Every dominant site in the Old Quarter and on Sovereign Arc was `instanced`, at
a depth exactly equal to the walker's own radius — the walker's *centre* inside
the box, not its shoulder clipping an edge — 1.0 to 1.5 m tall, and reported as
`no-props`, meaning nothing declared stood anywhere near. `whatis.mjs` named
them: a two-child group of instanced meshes, 215 to 256 triangles, 0.6 to 0.9 m
across. Tree crowns.

The arithmetic is conclusive and needs no measurement to see once stated. A
local street is .92 wide, so its kerb is at .46; `KERB_TO_PAVEMENT` is .28 and
`STREET_PAVEMENT_HALF` is .09, so the walkable band runs from .65 to .83 out
from the centreline. `addCityEnvironment` plants its street trees at **.82**,
with a crown reaching about .31. The tree is not beside the pavement. It is
standing in the middle of it, at chest height, and every walker who uses that
street walks through the foliage from one end to the other.

The repair is that the tree moves, and it is the mirror image of every arm that
failed: no pavement is removed, nothing is declared, no route is constrained.
`cutFootwaysAroundSolids` could not have helped even if the crowns had been
declared — the band is .09 wide and the crown is .31, so the cut would have
deleted the pavement rather than narrowed it, which is the documented dead end.

| district | before | after `d628aa6` | after `919b468` |
| --- | --- | --- | --- |
| Sovereign Arc | .1740 .2085 .1740 | .0400 .0400 .0488 | .0307 .0395 .0395 |
| Old Quarter | .0435 .0435 .0435 | .0109 .0109 .0021 | .0021 .0021 .0109 |
| The Circuit | .0427 .0427 .0427 | .0418 .0418 .0418 | .0418 .0418 .0418 |

Two thirds of the districts' run-to-run variance went with it: the noise was
walkers wandering in and out of foliage.

The second commit is the same fix for `createHorizonRing`'s faubourg ribbons and
hamlet windbreaks, which plant straight into an instanced field and bypass the
district's pass. It is handed a clearance field already and never gave it to the
trees. Sovereign Arc's south radial had a walker inside a windbreak at `12,-28`
for 101 frames of 900; that site is now 9 frames at a depth of .006.

Totals: 231 trees on Sovereign Arc, 85 moved and 34 felled; 294 in the Old
Quarter, 116 and 75; 310 on The Circuit, 37 and none. The Old Quarter's quarter
is the price of a medieval street plan with a .35 m pavement — there is nowhere
for those trees to stand.

### Nobody is stranded

`stranded.mjs` is new and exists for exactly the failure the other instruments
cannot see: a walker who cannot move is inside nothing, so constraining the
router improves every pedestrian number by stopping people going anywhere.

Baseline and both tree commits agree exactly, which is the expected result for a
change that does not touch the network: 804 / 43 / 171 ways, 1873.7 / 96.7 / 635
m, 118 / 32 / 66 of them with no walkable width. Parked walkers move by at most
one either way, in both directions, and median distance travelled is unchanged.

Worth recording on its own: **32 of The Circuit's 43 ways have no walkable width
at all**, and 118 of the Old Quarter's 804. That is the straitjacket, quantified.

## Three arms that made it worse, and why they are all the same failure

All three attacked The Circuit. All three left the two other districts inside
their own spread and all three moved The Circuit the wrong way, hugely.

| arm | what it changed | Circuit, walkers inside |
| --- | --- | --- |
| baseline | — | .0427 .0427 .0427 |
| A | anchored town lattice + civic fit + authored-parcel siting | .2436 .3476 .2741 |
| B | A without the lattice anchoring | .1219 |
| C | civic fit alone | .2396 |

(Arm B and arm C lost replicates to the hang described above. They are reported
as taken. Neither needed a third: a 3x and a 5.6x move on a district that
repeats to four digits is not a replicate problem.)

Preserved, not deleted, on `exp/anchored-lattice` and `exp/civic-fit`.

### What each arm was

- **Authored-parcel siting.** The tier offices and the rival compounds are the
  only solids on the map never reconciled with the *pedestrian* network — the
  planned buildings and the loose props both are, via `buildingCorridors`, which
  has always included the footways. Their sites come from the route curve and a
  fixed table; the towns then lay a full grid over the same ground. So: put the
  footways into the main clearance field too and site those parcels against it
  before anything is built on them.
- **Civic fit.** `createCourthouse` is 5.2 x 3.5 at scale 1, and
  `addCircuitTown` drops it at a fixed .74 (seat) or .44 (village) on whatever
  block the lattice happened to produce, with the market cross and the two lamps
  at fixed fractions of that block. Nothing checked the block had room. Cut them
  to the plot instead, exactly as the Old Quarter courthouse already is.
- **Lattice anchoring.** `assembleAxis` drops the closer of any two lines within
  `minGap` in favour of the higher street class, and a town's own bounding lanes
  are `alley`, the lowest class there is. Fenwick — the county seat — loses its
  eastern back lane to a jittered local street 1.79 away and its southern one to
  a chain of three merges, and comes out **two blocks**, with a 1.89 x 3.05
  "market place" carrying a 3.85 x 2.59 courthouse. Anchoring the six lines that
  define a town fixes that.

### Why they failed: the map is held together by the zero-width ways

The reading that fits every arm, including this morning's:

A `half: 0` way is not merely a bad path. It is a **straitjacket**. A walker on
a way with no lateral freedom is pinned to the centreline, and the centrelines
are, by construction, the middle of the street. The Circuit's .0427 baseline —
four contact sites, all grazing, worst depth .06, all of them a cattle trough —
is *achieved by* the collapsed bands, not in spite of them.

`cutFootwaysAroundSolids` narrows a band only around **declared** solids. Most
of The Circuit is not declared: the farmsteads, the church, the field gates, the
cottage interiors and everything `bakeBatches` merges are invisible to it and
fully visible to the per-triangle audit. So the moment a band is allowed to
reopen — by shrinking the courthouse standing on it, by moving a compound off
it, by restoring the back lane it belonged to — the walkers it was holding on
the centreline spread out into geometry nobody ever declared. Arm C is the
cleanest demonstration: the *only* thing it does is make three set-pieces
smaller, and it takes the district from .0427 to .2396.

This is the same lesson the brief draws from attempt three, one level down.
Attempt three concentrated traffic by removing pavement; these arms diffuse it
by restoring pavement. Both make walkers-inside worse, because the pavement was
never the thing keeping people out of buildings — the *absence* of pavement was.

The corollary is the one useful piece of direction to come out of today:
**declaring solids improves the metric and freeing bands worsens it.** The
stalls commit is that corollary landed. The order of work for the next agent is
therefore inverted from the brief's: run `undeclared.mjs` per district, declare
what it finds, and only then fix the civic fit and the parcel siting. Fixing the
geometry first, on this map, is measurably harmful.

### Retested on the cleaned map, and still no

My own recommendation was that these faults would pay off once the router could
see the geometry. With the trees off the pavements, the civic fit was applied
again on top of the two tree commits and measured three times:

| district | branch tip | with the civic fit |
| --- | --- | --- |
| The Circuit | .0418 .0418 .0418 | .1932 .1751 .1846 |
| Sovereign Arc | .0307 .0395 .0395 | .0249 .0307 .0308 |
| Old Quarter | .0021 .0021 .0109 | .0109 .0109 .0109 |

Better than the .2396 it measured before the trees moved, and still four times
worse than doing nothing. Reverted again.

The worst site names the reason, and it is not a mystery: `static@12,-8`, 562
frames of 900 at a depth of .275, which is the walker's whole radius. That is
**Ashgate's rival compound**, one of the sixteen authored parcels below. Shrink
the courthouse and Ashgate's pavements get their width back; the first thing the
width is used for is walking into the compound standing on them.

So the ordering holds, one step further out than I had it: the civic fit and the
lattice anchoring unlock nothing until the authored parcels are off the
pedestrian network, and the authored parcels cannot be nudged off it. That is
the next piece of work and it is a structural one.

### A separate finding: the parcels cannot escape at all

`authored.mjs` on arm A, with the footways in the clearance field:

| district | parcels | moved | stuck | worst intrusion |
| --- | --- | --- | --- | --- |
| The Circuit | 4 | 1 (by .40 m) | 3 | 2.10 |
| Old Quarter | 9 | 0 | 9 | 2.28 |
| Sovereign Arc | 4 | 0 | 4 | 2.50 |

Every authored landmark on the map stands 0.8 to 2.5 m inside the pedestrian
network and **sixteen of seventeen cannot find clear ground within 3.4 m**, with
twelve escape passes rather than the props' four. This is not a defect that
nudging can repair: the authored table and the street generators are laid on the
same ground and there is nowhere for the buildings to go. It also means arm A
was very nearly a no-op geometrically, which is worth knowing — its 6x
regression is almost entirely the lattice and civic changes, not the siting.

Note also that adding the footways to the field makes the *existing* rival-siting
call unsatisfiable, so compounds that used to clear the carriageway now clear
nothing and stay where they were authored. Anyone repeating this must keep the
road-only field for that call.

## The vehicle result does not currently hold, and did not before I started

`collide.mjs`, `MAPS_FRAMES=3600`, `inVehicle.bodyInsideFrames`:

| district | at `HEAD` (no change of mine) | with the stalls |
| --- | --- | --- |
| Old Quarter | 0 | 0 |
| The Circuit | — | 0 |
| Sovereign Arc | **227** (worst depth .185) | **51** (worst depth .072) |
| Ocean | — | 0 |

Sovereign Arc is not zero at the branch point. The stalls commit cannot reach
that district and the arm with it in is the better of the two, so this is a
pre-existing regression on `73e9f3e` rather than anything from this session, but
the banked win as stated — zero bodies inside vehicles across 3600 frames — is
not true of the map today. Sites: `road@-8,3` 39 frames, `road@9,9` 32,
`road@0,15` 30, `road@5,-22` 23.

### It was never true. There is no regression to bisect.

The figure is noisy — the same scene gives 51, 66, 141, 141, 187 and 227 across
runs, because it counts frames of contact and a single walker loitering at a
kerb dominates it — so the first move was to establish that, and the second was
to check the claim at its source rather than bisect towards it.

`3c7bc1b`, "Give map crowds real bodies and stop vehicles entering buildings",
is the commit the zero was banked from. Measured there, with this worktree's
harness and 3600 frames:

| district | at `3c7bc1b` | at `73e9f3e` | at this branch's tip |
| --- | --- | --- | --- |
| Old Quarter | 237 | 0 | 0, 0, 21 |
| The Circuit | 217 | 0 | 0 |
| Sovereign Arc | 339 | 227 | 141, 187, 66 |
| Treaty Sea | 0 | 0 | 0 |

`contacts` is non-zero at `3c7bc1b` in every case, so the instrument was reading
the scene and not failing open. **Every district was bad when the zero was
recorded, and the twenty commits since have been an improvement throughout.**
Two of the three are now genuinely zero and the third is down by two thirds.

So nothing regressed. The claim was a measurement of something narrower than the
sentence it turned into. Sovereign Arc's remaining sites are all pavement at a
crossing — `road@0,-19`, `road@0,15`, `road@9,9`, `road@0,14` — which is a
crossing-timing question and a different defect from the one this session is
about.

---

## Authoring the landmark sites from the street plan

The seventeen authored parcels — five headquarters and four rival compounds in
the Old Quarter, two and two on The Circuit, two and two on the Sovereign Arc —
were the only buildings in the game whose positions were written down rather
than planned. A table named a coordinate, the district then laid its streets,
and whichever of the two happened to be there won. Sixteen lost.

### What changed

`siteOnPlan` inverts the relationship. The authored position becomes a request;
the plan answers with the nearest ground it has actually left free. Three
details are what make it work where three previous escape passes did not:

1. **The declared footprint, oriented.** The same rectangle `createLevelBuilding`
   and `createRivalBuilding` mark solid, tested with `clearanceIntrusion`'s
   support-width path, so a site that clears at siting time cannot be found
   overlapping at routing time.
2. **`plannedGround`.** The terraces go into the same segment field as the
   pavements, taken from `buildingAudit`. Every earlier pass worked from
   `crowdObstacles`, which cannot see an instanced building, so it could move a
   compound out of a lane and into a cottage without noticing. A rectangle is
   carried as the corridor along its own longer axis with the shorter
   half-extent for a width — the stadium that contains it.
3. **An ordered sweep, not a gradient.** Two pavements meeting at a corner give
   a gradient a local minimum it cannot leave, which is exactly why the escape
   pass reported sixteen of seventeen parcels with no clear ground within 3.4 m.
   That was a false negative. It is anisotropic for a headquarters, which is set
   back from a named stretch of route: a deeper forecourt on the same street is
   cheap, a slide up the road is expensive (`lateralCost`).

### The line that mattered more than the siting

Moving a parcel silently stopped `clearAuthoredParcel` running where the table
had asked for it, leaving scenery standing that every previous build deleted —
226 extra solids on The Circuit. Clearing both the asked-for parcel and the
granted one is one line and it is the difference between a 30% regression and a
tenfold improvement. **A change that moves an object must also account for what
used to happen at the place it left.**

### Measured, `inside.mjs`, 900 frames, three replicates each

| district | control | sited, one clearing | sited, both clearings |
| --- | --- | --- | --- |
| Old Quarter | .0109 .0021 .0021 | .0021 .0021 .0021 | .0021 .0109 .0021 |
| The Circuit | .0418 .0418 .0418 | .0543 .0543 .0543 | **.0040 .0086 .0040** |
| Sovereign Arc | .0298 .0298 .0395 | .0206 .0136 .0329 | .0136 .0329 .0301 |

Old Quarter is a true control: all nine of its parcels are stuck, so nothing
in it moves. The Circuit is ten times better than the control. The Arc holds
its tree win.

### Where the landmarks went

| parcel | from | to | moved |
| --- | --- | --- | --- |
| `tier-6` | 11.39, 2.70 | 11.39, 3.15 | .45 |
| `rival-regional_firm` | -11.5, -8 | -16.45, -8 | 4.95 |
| `rival-national_competitor` | 11.5, -8 | 16.15, -8 | 4.65 |
| `rival-continental_rival` | -13, -7 | -17.05, -7.30 | 4.06 |
| `rival-oceanic_rival` | 13, -7 | 17.35, -7.15 | 4.35 |

The two Circuit compounds come out of Marlow and out of Ashgate onto the
village edge; the two Arc compounds come outside the wall ring, which is where
the region's own plan says commercial frontage belongs. No key, name, order or
district changes, and a compound's ring, anchor, travel anchor, guard and
collider all derive from the site, so they move with it.

### Stranding: the network got better, not smaller

| district | ways | length | no width | active | parked |
| --- | --- | --- | --- | --- | --- |
| Old Quarter, control | 804 | 1873.7 | 118 | 15/18 | 0 |
| Old Quarter, sited | 804 | 1873.7 | 118 | 15/18 | 0 |
| The Circuit, control | 43 | 96.7 | 32 | 7/9 | 0 |
| The Circuit, sited | 43 | 98.8 | **24** | **9/9** | 2 |
| Sovereign Arc, control | 171 | 635.0 | 66 | 11/14 | 0 |
| Sovereign Arc, sited | 165 | 645.3 | **60** | 11/14 | 0 |

Fourteen zero-width ways became walkable and both changed districts gained
length. That is the compounds coming off the pavement, and it is the opposite
of the concentration failure this class of change is prone to. The two parked
Circuit walkers had both travelled (minimum 5.76 units), so nobody is stuck.

### Vehicles

`inVehicle.bodyInsideFrames` over 900 frames: Old Quarter 0, The Circuit 0,
Sovereign Arc 28 against 227 at the branch point. Wrong-side frames zero
everywhere, facade contacts zero everywhere.

### The civic fit is now settled: it loses

Retested twice on top of this work, three replicates, The Circuit:

| arm | The Circuit |
| --- | --- |
| do nothing (sited map) | .0040 .0086 .0040 |
| civic fit on the one-clearing map | .0820 .0820 .0965 |
| civic fit on the finished map | .1856 .1461 .1089 |

Taking the parcels off the pedestrian network halved the civic fit's damage
relative to its earlier .1932/.1751/.1846, so the mechanism was real. It is
still twenty-five times worse than leaving the town plan alone. **Three
independent attempts on three different substrates have now measured the same
direction. It should not be attempted a fourth time without a new hypothesis.**

### What is left, with evidence

- **Street furniture is now the dominant remaining defect.** At full walker
  radius, `collide.mjs` names `nation-green-bench-1` (135 frames, .284 deep),
  `nation-milestone-1-e` (54) and `city-market-fountain`. These are declared
  props standing on pavement, the same fault class the tree pass fixed and
  fixable the same way. The standing warning against a blanket prop-clearance
  pass still applies; the tree pass shows the shape that works.
- **Eleven parcels still cannot be sited.** All five Old Quarter headquarters,
  all four of its compounds, and both Arc headquarters find nothing free within
  their search. For a headquarters this is structural: the route corridor is
  paved on both sides and the block behind is built out, so there is nowhere for
  a 2.9 by 2.55 building to stand. The next step is the ordering flip — the
  district reserves the parcel before it lays a street, so the *street plan*
  should honour the reservation, not just the building plan. `planFootways`
  is the one place the whole network is assembled and is where that belongs.
- **A bisect that is worth remembering.** Siting the compounds alone, with the
  headquarters left where they were, measured .1288/.1397/.1397 — three times
  worse than siting everything and three times worse than doing nothing. The
  cause was the uncontrolled clearing above. A single-parcel change moving the
  number by more than the change itself is the signature to watch for.

---

## Connections in the world, and a chosen district (session four)

### The stack changed under this session, and the baselines moved with it

The API on 5001 belongs to the main repository and predates the office
relationship wall, so `GameAsset.districts` — the districts a connection opens —
was simply absent from every catalog it served, and the join this work is built
on returned nothing. A worktree backend now runs on 5091 against a copy of the
live database taken with `sqlite3 .backup`, and Vite on 5291 proxies to it via
`LSAT_API_PORT`. **Every figure below was re-baselined on that stack.** The
account is the demo profile rather than the old dev firm — four connections
owned, no districts held — so the numbers are not comparable with earlier
sessions and the earlier ones should not be quoted against them.

| arm | Old Quarter | The Circuit | Sovereign Arc |
| --- | --- | --- | --- |
| before, three replicates | .0109 .0021 .0109 | .0086 .0086 .0040 | .0329 .0314 .0191 |
| after, three replicates | .0021 .0021 .0021 | .0086 .0086 .0086 | .0314 .0329 .0314 |

The Circuit and Sovereign Arc have no owned connection reaching into them, so
they are deterministic controls here and both hold. The Old Quarter, which
gained four contacts, settles on .0021 where it previously moved between .0021
and .0109 — no worse, and possibly steadier, though that is inside the noise
floor and is not claimed.

`stranded.mjs` is identical on every arm: 804 ways / 1873.7 m / 118 zero-width
and 15 of 18 walkers active on the Old Quarter, 43 / 98.8 / 24 and 9 of 9 on The
Circuit, 165 / 645.3 / 60 and 11 of 14 on the Arc. Nothing here touches the
network, which is the point: a contact is not declared solid, on the rival
guard's precedent.

Vehicles: `bodyInVehicleFrames` 0 / 0 / **127**, and the same 0 / 0 / 127 with
this branch's map files checked out at their previous commit. The banked figure
was 28 on the old account; the Arc's regression is real but it is in the
account, not in this change, and it reproduces with the change absent.

### Two blind spots, both silent, both found by looking rather than measuring

The first three contacts each reported `cleared` with `moved 0.00` and stood at
the exact centre of the district they belonged to — inside a tree apiece.

1. **`crowdObstacles` cannot see an instanced mesh.** Known for buildings, which
   is why `plannedGround` reads `buildingAudit`. Trees have the same problem and
   had no audit at all, so `recordTrees` now writes one the way
   `renderPlannedBuildings` writes its own.
2. **`prepareClearance` discards a corridor written as two identical points.**
   `if (length < 1e-4) continue`. A round obstacle expressed as a degenerate
   segment is dropped on the way into the field, the field builds without error,
   and every query against it answers "clear". `discCorridor` gives the segment
   a 2 mm length, which is the whole fix. Anything else adding a round keep-out
   to a clearance field should use it rather than rediscovering this.

### Scene cost

Measured with `tools/map-qa/cost.mjs`, which forces a frame first because the
synthetic clock otherwise leaves `renderer.info` reading a stale one-triangle
render.

| region | triangles before → after | draw calls | sprites |
| --- | --- | --- | --- |
| Old Quarter | 569,832 → 621,424 (+9.1%) | 1017 → 1031 | 12 → 16 |
| The Circuit | 199,796 → 199,796 | 382 → 382 | 6 → 6 |
| Sovereign Arc | 413,902 → 413,902 | 562 → 562 | 6 → 6 |

Four contacts cost about 51,600 triangles, and almost all of it is the four
people: a contact reuses `buildCrowdWalker` and rides in the same
`CrowdRenderer` the rival guards use, so it costs exactly what a pedestrian
costs and the district already draws fifteen of those. The shingles are five
boxes each and fold into the static batches. The name cards are hidden until
their district is asked about, so they cost four sprites and no draw call at
rest. If this has to come down, the lever is the figure, and the figure is the
thing the brief asked to look like the office cast.

### Street furniture: attempted, measured four to fourteen times worse, reverted

`furnitureOffPavement` moved every prop under a .62 footprint radius that was
not an enclosed mass and not a click target off the pedestrian ground, with the
tree pass's own limits — a 1.1 unit budget, eight passes, and nothing ever
deleted.

| arm | Old Quarter | The Circuit | Sovereign Arc |
| --- | --- | --- | --- |
| control | .0021 .0021 .0021 | .0086 .0086 .0086 | .0314 .0329 .0314 |
| furniture off pavement | .0131 .0043 .0043 | .0639 .1188 .0562 | .0136 .0314 .0219 |

The Circuit is seven to fourteen times worse and the Old Quarter twice as bad.
Reverted. This is the third time a pass that moves the small prop class has
measured worse than doing nothing, and it is now worth stating the likely
mechanism rather than only the result: a bench beside a path is furniture the
crowd steers around at a shoulder, and moving it off the paving puts it in the
open ground the crowd actually uses to pass one another. The tree pass works
because a crown at chest height over a .09 band leaves a walker no lateral
offset at all; a bench does. **Do not treat the two classes alike.** The named
sites — `nation-green-bench-1`, `nation-milestone-1-e`, `city-market-fountain` —
remain open, and the next attempt should move the *paving*, not the prop.

## The map and the retainer ledger, joined (on `integration/all-features`)

Merged `fix/map-generator` into `integration/all-features` in the demo
worktree. One conflict, in `unified-empire-map.tsx`, and it was two people
writing at the same *line* rather than at the same *thing*: the firm-tab pass
added a `focusDistrict` prop to `UnifiedEmpireMap`'s signature, and the map
pass inserted `DistrictBrief` immediately above that signature. Both kept
whole. Nothing else in the branch conflicted — worth recording that the other
two passes that landed today (`feat/firmux-pass`, `feat/perf-pass`) touched no
file under `frontend/src/art/` except `unified-empire-map.{tsx,css}`, so
`map-three-scene.tsx` is byte-identical to the commit every walker figure in
this document was measured at, and the merge cannot have moved them.

The join itself. Each surface already knew half of it and neither could see
the other half:

  ledger -> map   `?district=` existed. Since the landmark siting work it now
                  also lights the gold selection wash on arrival, because the
                  camera flight goes through `travelToLandmark`, which is what
                  sets the selection. Nobody wrote that; it fell out.
  map -> board    `selectedDistrict` is a `MapLandmark`; running it back
                  through `TerritoryDistrict.landmark_key` yields the ledger's
                  own key, which is exactly what the firm-tab pass's new
                  `highlightKey` wants. One derived value, both boards.
  map -> ledger   New. `onManage` carries an optional district key, the brief
                  has a link that sends it, and `RetainerLedger` takes a
                  `highlightKey` that picks the region as well as the row.

Vocabulary. Three schemes existed: the scene's gold `#e4c36e` for selected and
teal `#6cae98` for held; the map board's teal for held, gold for available and
*cyan* `#6fd0c5` for the row the ledger asked for; the ledger's green for held
and gold for money. The cyan was the odd one and it sat a few degrees from the
held teal, so a marked row and a held row read as the same state. Both
asked-for treatments are now selection gold. The `MapPin` button stays cyan
deliberately: it is the same affordance the connection and rival cards use, so
changing it would break a consistency to fix one.

Round trip verified on the demo stack (`tools/map-qa/join.mjs`, new): select
Quarter Courthouse on the ground -> board row marked, gold wash up -> "this
retainer in the ledger" -> `/firm?tab=connections&district=quarter_courthouse`,
Old Quarter region chosen, plot outlined gold -> pin -> `/map?district=...`
with the board open on the same row.

One trap for the next person writing a probe: `page.waitForFunction(fn, opts)`
puts the options in the *argument* slot. The wait then runs on its rAF default
polling, which the synthetic clock does not service, and it hangs for the
default thirty seconds rather than the timeout you thought you set. Options are
the third parameter; pass `null` second.

## The eleven unsitable parcels: root cause found, fix NOT landed (8:12–9:40 session)

Short version: the ordering flip in `planFootways` is **not** the fix, and
`planFootways` is not even in the right file. The parcels are stuck for two
reasons that are both upstream of it, both quantified below, and neither needs
a line of `map-agents.ts`. I ran out of session before I could measure a
change, so the tree is clean apart from tooling. **Nothing about the scene was
changed. The baselines are untouched.**

### What is actually stuck

`authored.mjs` on this branch, three regions, twelve parcels stuck of
seventeen:

| region | parcels | on asked | resited | stuck |
| --- | --- | --- | --- | --- |
| city (Old Quarter) | 9 | 0 | 0 | **9** — all five tiers and all four rivals |
| nation (The Circuit) | 4 | 0 | 3 | 1 (`tier-5`) |
| continent (Sovereign Arc) | 4 | 0 | 2 | 2 (`tier-10`, `tier-11`) |

### Why, measured rather than guessed

New probe, `tools/map-qa/blockers.mjs`, reports which *movement network*
records overlap a parcel — which is the question siting fails on, and which
neither `whatis.mjs` (triangles) nor `undeclared.mjs` (declared solids) can
answer. Run it as `node tools/map-qa/blockers.mjs city`; with no coordinates it
reads the stuck list out of the scene's own siting log.

**Fault one, the corridor building line.** Old Quarter's high street commits to
`KERB_OFFSET = .9` and `SETBACK = 1.95` in `addCityCorridor`, and records its
pavement centred at 1.43 with half-width .46 — so the paving runs out to 1.89
from the route centre and the shopfronts stand at 1.95. The tier table one
function away asks for a plinth centred at **2.80** with half-depth 1.275, so
its near face is at **1.525**, a third of a metre *inside* its own street's
pavement. Every one of the five is authored into the paving. No search radius
finds ground that was never left, which is why the failure looks like a siting
problem and is not one.

The fix is four lines and I wrote and typechecked it, then reverted it
unmeasured (see below). Publish the line where the corridor decides it:

```ts
// addCityCorridor, straight after recordCurveFootways(root, route, ...)
root.userData.corridorBuildingLine = SETBACK
```

and consume it where the table is read, instead of the typed figure:

```ts
const buildingLine = world.userData.corridorBuildingLine as number | undefined
const clearance = buildingLine === undefined ? setback[region] : buildingLine + 1.275 + WALKER_HALF_BEAM
const asked = roadPoint.clone().add(side.clone().multiplyScalar(Math.max(setback[region], clearance)))
```

That moves the Old Quarter headquarters from 2.80 to 3.385, and it is the same
principle as `streetHalfPaved`: the thing that lays the paving is the thing
that says where the building line is, so the two cannot disagree.

**Fault two, and this is the binding one.** With the setback corrected, all
nine city parcels are *still* stuck. The grid's north–south streets run
straight through the parcels. `tier-0` is authored at x = -10.64 and there is a
street at x = -10.42: its two pavements sit at -11.16 and -9.68, and the plinth
is 3.2 wide. It cannot fit between them, and clearing them outright needs a
lateral slide of about 2.8 against a budget of 1.2. The four rival compounds
are the same story — note that their ward blocks are *already* reserved
(`reserved` in `addCityEnvironment`, the hard-coded list at the four compound
coordinates), and `clearReserved` keeps planned buildings off them, but nothing
keeps the **street grid** off them. The reservation was only ever half a
reservation.

So the real ordering flip is one level up from where I put it in the last
handoff, and it is entirely inside files I own:

  today   `addPlannedStreets(streetsFromGrid(...))` lays every street and both
          its pavements across the whole lattice -> corridor runs -> tier and
          rival loops ask `siteOnPlan` to find a gap that was never left.
  wanted  derive the tier and rival parcels from the route curve *first*, put
          them in `reserved`, and have `recordStreetNetwork` split each
          pavement run — and, where it must, the carriageway — around them
          before recording, exactly as `onReserved` predicates already do for
          The Circuit's lanes (`map-three-scene.tsx` lines ~2723, 3719, 4124).
          The Circuit already does this and it is why only one of its four
          parcels is stuck rather than all four.

The authored positions are pure functions of `routeCurve` and the `authoredSides`
table, so they can be computed before the environment builders run at line
~7903 with no new information. That is the whole flip.

### Why I did not land it

The setback fix alone does not free a single parcel (measured: nine stuck
before, nine after), so shipping it would have been an unmeasured change to
five building positions with no win to justify it. Reverted. The grid
reservation is the half that pays and it is thirty minutes of careful work plus
three replicates a district, which I did not have.

**Do not run `inside.mjs` expecting the setback change to show.** It moves
buildings 0.585 m and frees nothing; it only becomes a win once the grid
reservation lets the parcels actually site.

### A hypothesis to kill

"The eleven parcels need a wider search." No. The lateral budget would have to
go from 1.2 to about 2.8 to clear a crossing street, and a parcel that slides
2.8 m has left the street it was authored against — which `authored.mjs` was
written to catch and already reports on The Circuit, where the two rival
compounds travelled 4.95 and 4.65. Widening the search converts a stuck parcel
into a parcel standing somewhere it does not belong, and the metric will not
tell you which you got.

### Environment, for whoever is next

The `ms-playwright` browser cache was deleted out from under a running
measurement this evening — disk has been at 99% all day — and every
browser-based tool on this machine died with "executable doesn't exist", which
reads like a broken harness rather than like the disk. `lib.mjs` now falls back
to the stock Chrome at `/Applications/Google Chrome/...`, which works and costs
no disk. Do not re-download the bundled Chromium; it is 400 MB of space
somebody deliberately freed. I also deleted four of my own screenshot
directories under `.maps` (19 MB to 7.3 MB).

My dev stack: Vite on **5373** against a backend on **5091** (the copied,
migrated QA database), started as `LSAT_API_PORT=5091 npx vite --port 5373`.
The port matters — a Vite started without it proxies to 5001, which is dead,
and the harness then hangs for three minutes on the dev-login button that never
renders.

### What I would do next, in order

1. Extract `authoredParcels(region, routeCurve, points)` from the tier and
   rival loops, returning key, centre, half-extents and frontage. Pure, no
   scene access.
2. Call it before the environment builders and merge the result into each
   region's `reserved` list. Old Quarter's is currently four hand-typed rival
   coordinates and no tiers at all; this replaces the typing.
3. Teach `recordStreetNetwork` to split a pavement run around a reserved site
   rather than recording it whole. Keep the drawn apron — a paving texture under
   a building is invisible and removing it is a second, separate risk.
4. Only then the setback derivation above, which stops the parcels being
   authored into the paving they are set back from.
5. Three replicates a district and `stranded.mjs` on every arm. Splitting
   pavement runs is exactly the change that can disconnect a network, and the
   third attempt of the day proved that removing pavement without giving the
   traffic somewhere to go measures worse even when the geometry looks better.

Job two, the street furniture with the paving moved instead of the prop, was
not started.

## The parcel reservation: built, measured, backed out (second session, 8:30–9:40)

Built exactly what the previous section said to build. It works mechanically
and it loses on the metric, so it is reverted — but reverted with `git revert`
rather than thrown away: **`git revert ccf6427` restores the whole thing** once
the missing half below is solved. Do not retype it.

### What was built

1. `authoredParcelReserve(region, route, points)` — the tier and rival parcels
   as `ReservedSite[]`, derived from the route curve and two tables that were
   previously inline in the two siting loops. Pure, no scene access.
2. Called at the top of the region build, **before** the environment builders,
   onto `world.userData.parcelReserve`.
3. `spanOutsideReserved()` in `recordStreetNetwork`: each pavement run is
   recorded as the pieces of itself that are not standing on a reserved parcel,
   with a `MIN_RUN` of .8 so no unreachable stub is left.
4. The corridor and the landmarks fronting it share one `CORRIDOR_BUILDING_LINE`
   (1.95 for the city) instead of two numbers a third of a metre apart.

### It did what it was built to do

`blockers.mjs` before: grid pavements overlapping every Old Quarter parcel, the
nearest at gap −0.04. After: **no footway overlaps any parcel** — the nearest is
the corridor at gap 1.49, outside the plinth's 1.275 + .16. The setback fix also
lands, moving the headquarters from 2.80 to 3.385.

### And it still loses, badly

`inside.mjs`, three replicates, identical to four digits each time (the scene is
deterministic, so a single figure per arm is the honest report here):

| arm | Old Quarter | The Circuit | Sovereign Arc |
| --- | --- | --- | --- |
| baseline | .0021 | .0040 | .0314 / .0329 / .0314 |
| reservation | **.0379** | **.1049** | .0219 |
| + per-piece street ids | **.0379** | **.0704** | not run |
| after revert (control) | .0021 | .0040 | — |

Eighteen times worse on the Old Quarter and twelve on The Circuit. Sovereign Arc
improved, which is consistent with its parcels being the two that were already
nearly sitable. The revert restores both figures exactly, so the measurement is
sound and the cause is the change.

### Why, and this is the part worth keeping

**The parcels are still stuck.** Nine of nine, before and after. Reserving the
*pavement* does not reserve the *carriageway*, and the grid's north–south
carriageways run through the parcels too: `tier-0` sits at x = −10.64 with a
street at −10.42, half .46, overlapping by .24; `tier-2` has a 0.97-half arterial
at gap 0.02. So the buildings did not move — and the pavement under them was
taken away while they stayed. That is the same shape as the three earlier
failures: **removing pavement from under a building that is still standing there
has now measured worse four separate times.** It is not a coincidence and it is
not the noise floor. Stop testing it.

One real bug was found and fixed on the way, and it is worth keeping in mind for
any future cut: the two pieces either side of a cut-out parcel were initially
given the *same* `street` id, and a shared id is precisely what tells the crowd
that two pavement ends facing each other are the two kerbs of one street. It
paired them into a crossing straight through the reserved parcel. Giving each
piece its own id took The Circuit from .1049 to .0704 by itself — a third of the
damage was walkers crossing the building. The remaining two thirds are not that.

### What the next agent should type first

The reservation is necessary and not sufficient. The missing half is that the
parcel has to end up somewhere the *carriageways* do not go, and there are only
two honest ways to do that:

  (a) Choose the parcel's position from the plan instead of from an even split
      of the route curve. `tierParcelSite` puts level *i* at
      `t = .12 + i / (n - 1) * .76`, which is a number with no relationship to
      where the streets are; landing it mid-block instead would remove the
      conflict at source rather than cutting anything. This is the one I would
      do, and `tierParcelSite` now exists as the single place to do it in.
  (b) Split the carriageways too. Cheaper to write and much more dangerous —
      the driving graph is what `portal` and the junction welding depend on,
      and a hole in it is a class of failure nobody has measured today.

Do **not** widen the search instead; that hypothesis is dead and the reasoning
is two sections up. And whatever is tried, the parcel must actually move — the
lesson of today is that clearing ground around a building that stays put makes
things worse every single time.

`stranded.mjs` was not run on the reverted arm: at eighteen times the baseline
there was nothing a connectivity check could have changed about the verdict, and
the session ended. Run it on the next arm.

Street furniture (move the paving, not the prop) was not started, again.

## Mid-block siting, and a measurement confound that undermines tonight's arms

### READ THIS FIRST: the dev server must be restarted before every arm

The control run at the end of this session, on a tree byte-identical to the
committed baseline, read **.0109 / .0086** where the same tree had read
**.0021 / .0040** an hour earlier. Same code, same probe, same machine. The
difference was that the second server had hot-reloaded through six edits.
Restarting Vite and re-running restored .0021 / .0040 exactly.

So **HMR-accumulated module state changes the scene**, and every arm measured
after an edit without a restart is inflated by an unknown amount. The static
geometry counts are unaffected (`solids 47912` matched to the unit both times),
so this is not the scene being rebuilt differently — it is something the crowd
or the network carries across a reload.

That is a five-fold inflation on the Old Quarter from nothing at all. **Every
figure in the two sections above was taken on a hot-reloaded server**, so the
"eighteen times worse" verdict on the reservation arm is not safe. It was still
clearly worse — .0379 against a .0109 contaminated control is 3.5x rather than
18x — but the honest position now is that the reservation arm has *not been
measured properly*, and the next agent should re-run it cold before accepting
the negative result. `git revert` of the two backouts brings it back.

Method from here: kill and restart Vite, then measure. Do not trust a figure
taken after an edit to the same server.

### The mid-block change itself

`tierParcelSite` now treats the even spread along the route as a preference
rather than a coordinate: it searches about one block either side of the ideal
`t` for the position with the most clearance from the region's cross-street
lines, and only the Old Quarter has a declared lattice to answer with, so only
it is affected. This is a choice about where a building is *authored*, made
before anything is built; it is not the search-widening hypothesis, which stays
dead.

It works. The five headquarters moved from -10.64, -5.32, 0, 5.32, 10.64 to
-12.32, -5.60, -0.42, 6.58, 10.78, and `blockers.mjs` shows them between
avenues rather than across one.

### And it is still not enough, for a reason that ends this line of attack

**The Old Quarter's headquarters does not fit between its avenues.** A tier-0
plinth is 1.45 half-width and needs 1.61 clear either side, so 3.22 m of free
width. The avenues flanking the corridor sit 4.08 apart and their kerbs leave
**2.94**. There is no position for it — not a search failure, a fact about the
district. All nine parcels stayed stuck, and the arm measured .0568 (worse than
the reservation alone at .0379 on the same contaminated basis). `stranded.mjs`
on this arm: 1 of 18 walkers parked, .0667, which is not a disconnection but is
not clean either.

### What is left, and it is now a design question rather than a bug

One of these has to give, and none of them is a fix to the siting code:

  (a) The headquarters gets narrower on the Old Quarter. 2.9 m of frontage is a
      townhouse, which is what a first office on a historic high street would
      actually be. Cheapest, and it makes the district read better rather than
      worse.
  (b) The avenue lattice is planned around the parcels. `planAxisInterior`
      generates the Old Quarter's interior avenue positions procedurally — only
      the three arterials at -18.6, 1 and 18.8 are named — so the filler lines
      could be nudged off a reserved parcel at plan time. This is the same
      ordering principle carried one level further up, and it is the one I would
      try, but the lattice is a module constant evaluated at import while the
      parcels depend on the route curve, so it needs the city's route to become
      a module constant first.
  (c) Accept that the Old Quarter's headquarters stand on the network and stop
      spending on it. Defensible: it is .0021 today, the best district on the
      map, and every attempt to improve it has cost more than it returned.

Do (a) before (b). And re-measure the reverted arms cold before anything else,
because the numbers they were judged on are not trustworthy.

## Session end, 9:40: what is true and what is not

Tonight's last forty minutes went on re-establishing the ground rather than on
the fix, which was the right trade: the fix was going to be judged by a metric
that turns out to disagree with itself.

**Cold re-run of the reverted arms: not completed.** I took a cold control, got
.0109 where the same tree had read .0021 an hour before, and spent the rest of
the time characterising that instead — because an arm measured against a control
that can be either of two values is not a measurement. Then the browser started
crashing outright, losing runs. The verdicts on both reverted arms therefore
stand as *unproven in both directions*: the reservation may or may not have lost
by eighteen times, and nobody currently knows.

**The per-piece street id fix is not separable, which I had not noticed.**
`spanOutsideReserved` returns the whole run untouched when nothing is reserved,
so there is only ever one piece and `piece === 0` always holds: without the
reservation the id fix is inert and cannot be landed or measured on its own. It
is a correct fix and it belongs with the reservation whenever that comes back.
The reasoning stands on its own merits — a shared id is what tells the crowd two
pavement ends are the kerbs of one street, and across a cut-out parcel that is a
crossing through a building — and it moved The Circuit .1049 to .0704, though on
the contaminated basis above.

**The Old Quarter width question is left for the user**, as asked. The finding
is that a tier-0 plinth needs 3.22 m of clear width and the avenues flanking the
corridor leave 2.94, so no siting code can place it. My recommendation is to
narrow the Old Quarter headquarters to roughly a 2.6 m frontage: a first office
on a historic high street *is* a townhouse, so this reads better rather than
worse, and it is the only option that does not involve either deleting streets
or replanning the lattice. But it changes what the player's first office looks
like, so it is theirs to decide.

### Order of work for whoever is next

1. Find the footway-network bimodality. Nothing else in this file can be trusted
   until it is fixed, and everything becomes cheaper once it is.
2. Re-run the two reverted arms cold, with in-session controls. `git revert` the
   two backout commits brings both back.
3. Then the width decision, if the user has made it.
