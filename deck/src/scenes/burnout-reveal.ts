/**
 * 10 → 11. Fade everything off the shared navy cyclorama, hold the empty
 * room, then ease the counsel lockup on. Not a consume, not a fire wipe,
 * and not the counsel pull — that override is 11 → 12 and is not touched.
 */
export const BURNOUT_REVEAL_MS = 1240
/** Stage swap once the outgoing items are gone and the navy has been held. */
export const BURNOUT_REVEAL_MID = 0.55

export const BURNOUT_REVEAL_FROM = 'pov-volume-burns'
export const BURNOUT_REVEAL_TO = 'concept-lawyer-tycoon'
